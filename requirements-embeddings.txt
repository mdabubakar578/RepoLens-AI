-r requirements.txt
# Similarity runs as a numpy matrix multiply over at most a few thousand
# chunks, so no approximate-nearest-neighbour index is needed. faiss-cpu was
# previously required here and, being hard to install, kept the semantic path
# switched off on most machines.
sentence-transformers>=3,<6
